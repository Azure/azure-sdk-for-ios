import XCTest
@testable import AzureSDKSwiftPM

final class AzureSDKSwiftPMTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AzureSDKSwiftPM().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
